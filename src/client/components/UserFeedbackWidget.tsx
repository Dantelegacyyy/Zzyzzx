import React from 'react';

/**
 * Purged per zero-distraction policy.
 * Permanently disabled: no floating feedback widgets or badges.
 */
export const UserFeedbackWidget: React.FC = () => {
  return null;
};
