import { initializeApp } from 'firebase-admin/app';
import { getAuth } from 'firebase-admin/auth';
initializeApp({ projectId: 'dependable-period-7ds98' });
console.log('Firebase initialized. Test passed!');
